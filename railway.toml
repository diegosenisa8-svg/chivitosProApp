# Servicio FRONT (Root Directory = /)
# Usa Dockerfile en la raíz (más estable que Nixpacks + npm ci doble)
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
