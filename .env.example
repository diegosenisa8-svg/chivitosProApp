# Local con proxy Vite (vacío = same-origin /api → 8080)
VITE_API_URL=

# Opcional: Client ID de Google (si falta, el front lo pide al API)
# VITE_GOOGLE_CLIENT_ID=123456789-xxxx.apps.googleusercontent.com

# Railway Web (runtime en server.mjs) — apunta al servicio API
# API_URL=https://TU-API.up.railway.app
